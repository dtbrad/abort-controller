{
  "name": "abort-controller",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bluebird": "^3.5.3",
    "concurrently": "^4.1.0",
    "lodash.debounce": "^4.0.8",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "react-redux": "^6.0.0",
    "react-scripts": "2.1.3",
    "redux": "^4.0.1",
    "redux-thunk": "^2.3.0"
  },
  "scripts": {
    "start": "concurrently \"react-scripts start\" \"json-server --watch --delay 1000 db.json --port 3001\" \"json-server --watch --delay 500 db.json --port 3002\"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "json-server": "^0.14.2"
  }
}
